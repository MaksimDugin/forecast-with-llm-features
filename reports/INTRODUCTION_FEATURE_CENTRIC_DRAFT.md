# INTRODUCTION_FEATURE_CENTRIC_DRAFT.md

Дата: 2026-06-26  
Статус: сгенерировано  
Фокус: признаки, теоретическое обоснование и влияние на прогноз.

---

# Введение

Финансовые рынки представляют собой информационно насыщенную среду, в которой цены активов изменяются под влиянием не только прошлой ценовой динамики, но и новостей, ожиданий инвесторов, аналитических комментариев и общего информационного фона. По этой причине прогнозирование финансовых временных рядов не может сводиться только к обработке исторических цен: часть потенциально значимой информации существует в текстовой форме и не всегда напрямую отражается в стандартных рыночных показателях.

В классических постановках задачи прогнозирования в качестве признаков часто используются цены открытия и закрытия, максимумы и минимумы за период, объёмы торгов, доходности, лаговые изменения и технические индикаторы. Эти признаки описывают уже наблюдавшееся поведение рынка. Однако текстовые источники могут содержать иную информацию: оценки событий, реакцию участников рынка, ожидания относительно будущей динамики, упоминания компаний, продуктов, регуляторных решений или макроэкономических факторов. Поэтому возникает вопрос, можно ли преобразовать финансовые тексты в числовые признаки и использовать их для повышения качества прогноза.

Развитие языковых моделей сделало эту постановку особенно актуальной. Современные модели обработки естественного языка позволяют получать не только простые оценки тональности, но и embedding-представления текстов. Такие представления могут сохранять больше информации, чем отдельный sentiment-score, поскольку отражают контекст, лексику и смысловые связи внутри сообщения. Для финансовой области важны доменные модели, в частности FinBERT, адаптированные к финансовым текстам. В данной работе FinBERT рассматривается не как самостоятельная модель прогнозирования цены, а как инструмент генерации признаков для последующей модели машинного обучения.

Актуальность исследования связана с необходимостью проверить, обладают ли LLM/FinBERT-generated features добавочной предсказательной силой относительно стандартных рыночных признаков. Иными словами, важно не просто построить модель прогноза, а оценить вклад разных групп признаков: что дают только рыночные признаки, есть ли самостоятельный сигнал в текстовых признаках и улучшается ли прогноз при объединении рыночной и текстовой информации.

Объектом исследования являются признаки, используемые в задачах прогнозирования финансовых временных рядов.

Предметом исследования является влияние признаков, извлечённых из финансовых текстов с помощью FinBERT, на качество прогноза направления движения цены акции AAPL на следующий торговый день.

Цель работы — теоретически обосновать использование LLM/FinBERT-generated features в задаче прогнозирования финансовых временных рядов и эмпирически оценить их влияние на качество прогноза направления цены.

Для достижения цели решаются следующие задачи:

1. Рассмотреть, какие группы признаков используются в задачах прогнозирования финансовых временных рядов.
2. Обосновать, почему финансовые тексты могут рассматриваться как источник признаков для прогноза.
3. Описать различие между рыночными признаками, sentiment-признаками и embedding-признаками.
4. Обосновать использование FinBERT как инструмента извлечения признаков из финансовых текстов.
5. Сформировать три группы признаков: prices-only, FinBERT-only и prices + FinBERT.
6. Построить экспериментальный протокол, позволяющий сравнить влияние этих групп признаков на прогноз.
7. Оценить качество моделей на каждой группе признаков с использованием единого хронологического разбиения данных.
8. Проверить, дают ли FinBERT-признаки самостоятельный сигнал и добавочную предсказательную силу относительно рыночных признаков.
9. Сформулировать ограничения полученных результатов и направления дальнейшей проверки.

Методологически работа строится как feature-centric эксперимент. Модели используются не как самостоятельный объект исследования, а как инструмент проверки информативности признаков. Поэтому основное внимание уделяется не максимизации accuracy любой ценой, а сопоставлению качества на разных группах признаков при едином протоколе разбиения, нормализации и оценки.

Эмпирическая часть работы выполнена на данных по акции AAPL. Целевая переменная задаётся как бинарный индикатор направления изменения цены закрытия на следующий торговый день. Для контроля утечки данных используется хронологическое разбиение train/validation/test без пересечения дат между подвыборками. Нормализация числовых признаков обучается только на обучающей выборке. Такой протокол позволяет оценивать именно вклад признаков, а не эффект некорректной подготовки данных.

Структура работы соответствует поставленной цели. В первой части рассматриваются подходы к прогнозированию финансовых временных рядов, роль текстовых данных и теоретическое обоснование LLM-generated features. Во второй части описываются данные, группы признаков, методология сравнения и модели как инструмент проверки признаков. В заключительной части анализируется, как разные группы признаков повлияли на качество прогноза, и формулируются ограничения исследования.
