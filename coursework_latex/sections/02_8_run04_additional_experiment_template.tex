\subsection{Дополнительный эксперимент 1: расширение рыночного baseline и снижение размерности}

Для углубления анализа был проведён дополнительный эксперимент RUN 04. Его цель состоит в том, чтобы проверить, зависит ли вывод о добавочной силе FinBERT-признаков от высокой размерности embedding-пространства и от состава рыночного baseline. В отличие от основного эксперимента RUN 03, где сравнение было укрупнённым, RUN 04 разделяет признаки на несколько групп: исходные ценовые признаки, rolling-признаки, признаки торгового объёма, полный FinBERT embedding, PCA-сжатые FinBERT embeddings и объединённые группы признаков.

Эксперимент был построен на данных \texttt{TheFinAI/flare-sm-bigdata} для тикера AAPL. Было извлечено 497 текстов; итоговый датасет содержит 163 наблюдения и 163 уникальные даты за период 2020-03-09 --- 2020-10-27. Хронологическое разбиение имеет вид:

\begin{table}[htbp]
\centering
\begin{tabular}{lrrl}
\toprule
Подвыборка & Наблюдения & Даты & Период \\
\midrule
Train & 114 & 114 & 2020-03-09 --- 2020-08-18 \\
Validation & 24 & 24 & 2020-08-19 --- 2020-09-22 \\
Test & 25 & 25 & 2020-09-23 --- 2020-10-27 \\
\bottomrule
\end{tabular}
\caption{Хронологическое разбиение данных в дополнительном эксперименте RUN 04}
\label{tab:run04_split}
\end{table}

В RUN 04 сохраняется та же постановка целевой переменной:

\[
y_t = \mathbf{1}\{Close_{t+1} > Close_t\}.
\]

Текстовые признаки строятся консервативно: для даты принятия решения $t$ используются только тексты не позднее $t-1$ в окне до 10 дней. Это позволяет снизить риск look-ahead bias.

Проблема полной embedding-размерности сохраняется: FinBERT embedding имеет размерность

\[
e_t \in \mathbb{R}^{768}.
\]

Поэтому в RUN 04 дополнительно строятся PCA-сжатые представления. Пусть $E_{train}$ --- матрица FinBERT embeddings на обучающей подвыборке. После центрирования строится ковариационная матрица

\[
\Sigma = \frac{1}{n_{train}-1}E_{train}^{\top}E_{train}.
\]

Первые $k$ собственных векторов образуют матрицу $V_k$, а сжатое представление имеет вид

\[
e_t^{(k)} = V_k^\top(e_t-\bar e_{train}),
\qquad
k \in \{16,32,64\}.
\]

PCA обучается только на обучающей подвыборке, чтобы избежать утечки информации из валидационной и тестовой подвыборок. После этого одно и то же преобразование применяется к валидационной и тестовой подвыборкам без повторной подгонки. Доля объяснённой дисперсии для $k=16$ составляет 0.984, для $k=32$ --- 0.997, для $k=64$ --- 0.9998. Это означает, что большая часть вариации FinBERT embeddings сжимается в небольшое число компонент, однако сохранение дисперсии не гарантирует сохранение прогнозного сигнала.

В эксперименте сравниваются следующие группы признаков:

\begin{table}[htbp]
\centering
\begin{tabular}{lp{9cm}}
\toprule
Группа & Состав \\
\midrule
\texttt{majority} & наиболее частый класс в обучающей подвыборке \\
\texttt{previous\_direction} & прогноз $\hat y_t = y_{t-1}$ \\
\texttt{prices\_raw} & 11 исходных ценовых признаков \\
\texttt{returns\_rolling} & доходности, rolling mean, rolling volatility, residuals \\
\texttt{volume\_rolling} & log-volume, relative volume, volume z-score \\
\texttt{market\_full} & 40 расширенных рыночных признаков \\
\texttt{finbert\_full} & 768 FinBERT embedding-компонент \\
\texttt{finbert\_pca\_k} & PCA-сжатые FinBERT embeddings \\
\texttt{market\_full\_finbert\_pca\_k} & расширенные рыночные признаки + PCA-сжатые embeddings \\
\bottomrule
\end{tabular}
\caption{Группы признаков в дополнительном эксперименте RUN 04}
\label{tab:run04_feature_groups}
\end{table}

Гиперпараметр $C$ для логистической регрессии и классификационный порог $\tau$ выбирались только на валидационной подвыборке. На тестовой подвыборке проводилась финальная оценка. Для большинства лучших рыночных моделей был выбран сильный регуляризатор $C=0.01$ и порог $\tau=0.75$.

Основные результаты приведены в таблице~\ref{tab:run04_results}.

\begin{table}[htbp]
\centering
\begin{tabular}{lrrrr}
\toprule
Группа признаков & Accuracy & Balanced acc. & F1 & ROC-AUC \\
\midrule
\texttt{majority} & 0.520 & 0.500 & 0.684 & 0.500 \\
\texttt{previous\_direction} & 0.360 & 0.359 & 0.385 & 0.359 \\
\texttt{prices\_raw} & 0.520 & 0.500 & 0.684 & 0.801 \\
\texttt{returns\_rolling} & 0.520 & 0.538 & 0.143 & 0.750 \\
\texttt{market\_full} & 0.560 & 0.577 & 0.267 & 0.853 \\
\texttt{finbert\_full} & 0.520 & 0.500 & 0.684 & 0.500 \\
\texttt{finbert\_pca\_32} & 0.520 & 0.500 & 0.684 & 0.500 \\
\texttt{market\_full\_finbert\_pca\_32} & 0.560 & 0.577 & 0.267 & 0.846 \\
\texttt{market\_full\_finbert\_pca\_64} & 0.520 & 0.500 & 0.684 & 0.795 \\
\bottomrule
\end{tabular}
\caption{Основные результаты RUN 04 на тестовой подвыборке}
\label{tab:run04_results}
\end{table}

Лучший результат по accuracy и balanced accuracy показывает группа \texttt{market\_full}: accuracy равна 0.560, balanced accuracy равна 0.577, ROC-AUC равен 0.853. Это означает, что расширенные рыночные признаки, включающие rolling-динамику и признаки объёма, дают более сильный baseline, чем исходная группа \texttt{prices\_raw}.

Добавочная сила PCA-сжатых текстовых признаков относительно \texttt{market\_full} не подтверждается. Для $k=32$ получаем:

\[
\Delta_{text}^{Accuracy}(32) = 0.560 - 0.560 = 0,
\]

\[
\Delta_{text}^{Balanced\ Accuracy}(32) = 0.577 - 0.577 = 0,
\]

\[
\Delta_{text}^{ROC\text{-}AUC}(32) = 0.846 - 0.853 = -0.007.
\]

Для $k=64$ результат хуже:

\[
\Delta_{text}^{Balanced\ Accuracy}(64) = 0.500 - 0.577 = -0.077.
\]

Следовательно, после снижения размерности FinBERT embeddings не дают устойчивого улучшения относительно расширенного рыночного baseline. Текстовые признаки отдельно также не превосходят \texttt{majority baseline}: \texttt{finbert\_full}, \texttt{finbert\_pca\_16}, \texttt{finbert\_pca\_32} и \texttt{finbert\_pca\_64} имеют balanced accuracy 0.500 и ROC-AUC 0.500.

Отдельно следует отметить различие между ROC-AUC и threshold-dependent метриками. У \texttt{market\_full} ROC-AUC равен 0.853, что говорит о способности модели ранжировать наблюдения по вероятности положительного класса. Однако F1 равен только 0.267, а recall составляет 0.154, поскольку подбор на валидационной подвыборке выбрал высокий порог $\tau=0.75$. Поэтому результат RUN 04 лучше интерпретировать как наличие ранжирующего сигнала в расширенных рыночных признаках, но не как устойчиво сильную бинарную классификацию при фиксированном пороге.

Итог RUN 04 усиливает основной вывод работы: расширение рыночных признаков улучшает baseline, но добавочная предсказательная сила FinBERT-generated features не подтверждается ни в полной embedding-размерности, ни после PCA-сжатия. Иными словами, эффект RUN 03 не сохраняется при усилении рыночного baseline. Это не означает, что текстовые признаки принципиально бесполезны; скорее, в данной постановке, для одного актива AAPL, одного горизонта прогноза и агрегированных tweet embeddings их вклад оказался слабее вклада рыночных и volume-based признаков.
